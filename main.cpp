# include <iostream>
using namespace std;

int main() {

    // Declaring Variables
    // system("clear");
    int height;
    int length;


    // Fetching User Input
    cout << "Input size of diamond: " << endl;
    cin >> height;


    // Constructing Diamond
    for (int i; i <= height; i++) {
        for (int a; a <= length; a++) {
            cout << "hi";
    }
    }

    return 0;
}